# React Web Application

## UI components

- when possible re-use existing shadcn components in `src/components/ui` install new shadcn components when needed
- build a custom shared view components in `src/components/ui` when shadcn does not have one
- layout specific components should be created in `src/components/layout`
