{
  "name": "rfq-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm exec wrangler dev",
    "test": "pnpm exec vitest run",
    "db:migrate": "pnpm exec wrangler d1 migrations apply rfq-server-db",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^4.0.15",
    "wrangler": "4.98.0"
  },
  "dependencies": {
    "hono": "^4.12.23"
  }
}
